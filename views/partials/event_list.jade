h2 Events
.table-responsive
    table.table.table-striped
        tbody
            tr(ng-repeat='event in events')
                td {{ pad(getDate(event.event_date).getDate(), 2) + '.' + pad(getDate(event.event_date).getMonth(), 2) + ' ' + pad(getDate(event.event_date).getHours(), 2) + ':' + pad(getDate(event.event_date).getMinutes(), 2) }}
                td
                    a(ng-href='events/{{event.uuid}}/{{event.access_token}}') {{event.title}}